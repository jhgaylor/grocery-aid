apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: grocery-aid-tls
  namespace: grocery-aid
spec:
  secretName: grocery-aid-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - grocery-aid.inevitable.fyi
